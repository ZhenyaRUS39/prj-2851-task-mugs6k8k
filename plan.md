# Plan: prj-2851-task-mugs6k8k

**Project:** `xml-parser-script`  
**Task ID:** `task-mugs6k8k`  
**Repo:** `prj-2851-task-mugs6k8k`  

## Summary

Декомпозиция задачи на XML-парсер: создание структуры проекта, реализация парсера с обработкой ошибок, тесты на типовые XML-сценарии, документация и интеграционный smoke-test.

## Subtasks

### 1. Инициализировать структуру проекта

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать репозиторий prj-2851-task-mugs6k8k с базовой структурой: parser.py (точка входа), xml_parser/ (модуль парсера), tests/ (каталог тестов), requirements.txt, .gitignore для Python. Инициализировать git, сделать первый коммит в main.
- **Test plan:** git status показывает чистое дерево; ls -la показывает parser.py, xml_parser/, tests/, requirements.txt; python -c 'import xml_parser' не падает (после sub-3).

### 2. Реализовать ядро XML-парсера

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В xml_parser/core.py реализовать функции: parse_file(path) -> dict, parse_string(xml_str) -> dict, validate_schema(data, xsd_path) -> bool. Использовать stdlib (xml.etree.ElementTree, xmlschema или lxml). Обработка ошибок через кастомные исключения XMLParseError, XMLValidationError. Логирование через logging. CLI-режим через argparse (--input, --output, --validate, --xsd).
- **Test plan:** python parser.py --input sample.xml отрабатывает без ошибки; при битом XML поднимает XMLParseError с понятным сообщением; --xsd флаг валидирует схему и возвращает exit code 0/1.
- **Dependencies:** `sub-1`

### 3. Написать unit-тесты

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В tests/ добавить test_parser.py с покрытием: парсинг валидного XML, обработка битого XML (XMLParseError), валидация по XSD-схеме, парсинг вложенных структур, обработка namespaces, парсинг пустого файла, обработка XML с CDATA. Использовать pytest. Минимальное покрытие — 80%.
- **Test plan:** pytest -v проходит все тесты зелёным; pytest --cov=xml_parser --cov-report=term-missing показывает coverage >= 80%.
- **Dependencies:** `sub-2`

### 4. Документация и smoke-test

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать README.md с описанием установки, использования, примерами CLI и API. Создать sample.xml и sample.xsd в examples/. Прогнать smoke-test: запустить парсер на sample.xml, убедиться что вывод корректный. Финальный коммит в main.
- **Test plan:** README рендерится корректно; python parser.py --input examples/sample.xml --output /tmp/out.json успешно создаёт валидный JSON; git log показывает все коммиты проекта.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
